# Required: create a v2 key at https://www.abuseipdb.com/account/api
ABUSEIPDB_API_KEY=replace-with-your-api-v2-key

# Recommended whenever Streamable HTTP is reachable by anything except localhost.
MCP_HTTP_BEARER_TOKEN=replace-with-a-long-random-token
